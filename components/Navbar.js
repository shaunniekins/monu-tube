import { Box, Card, Flex, Text } from "@chakra-ui/react";

const Navbar = () => {
  return (
    <Flex>
      <Text as={"b"} fontSize={"2xl"}>
        MONU-TUBE
      </Text>
    </Flex>
  );
};

export default Navbar;
